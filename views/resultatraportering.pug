extends layout
block resultatraportering
    h1 Choose your church & vote here
    form#form
        select#first
            option Blue Group 
            option Green Group
    
        button  Submit

        script.

            const form = document.getElementById('form');
            const first = document.getElementById('first');
            
            form.addEventListener('submit',function(){
            console.log('submit is clicked');
            
            let vote = { first: first.options[first.selectedIndex].value,
           
            };
            console.log('vote is '+ vote.first);
            socket.emit('vote update', vote.first);
            event.preventDefault();
            return false;        
            });    
